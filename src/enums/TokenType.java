package enums;

public enum TokenType {
    NUMBER, STRING, BOOL,
    LPARENT, RPARENT,
    LBRACE, RBRACE,
    SEMICOLON, ASSIGN, COMMA, RETURN,
    PLUS, MINUS, MUL, DIV,
    IDENT,
    PRINT, IF, ELSE, WHILE, FUNCTION,
    GREATER, LESS, EQUAL, GREATEREQUAL, LESSEQUAL, NOTEQUAL,
    AND, OR, NOT
}
