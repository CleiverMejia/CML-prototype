## What does it include?

- data types such as `number` and `string`

- creation of variables `name = "cleiver";`

- display messages in the console `print("Hello world!");`

- conditional if (else not yet added) `if (name == "cleiver") {}`

## Note

- Equal can only be used in the if statement.